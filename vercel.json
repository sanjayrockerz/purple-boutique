{
  "framework": "vite",
  "buildCommand": "cd naatu-shop && npm install && npm run build",
  "outputDirectory": "naatu-shop/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
